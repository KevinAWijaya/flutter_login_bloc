name: wisdom_pos_test
description: A new Flutter project.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.1.2 <4.0.0"

dependencies:
  flutter_bloc: ^9.1.1
  dio: ^5.9.0
  equatable: ^2.0.7
  flutter:
    sdk: flutter
  google_fonts: ^6.2.0
  intl: ^0.19.0
  hydrated_bloc: ^10.1.1
  path_provider: ^2.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

dependency_overrides:
  win32: ^5.5.4 # Force package using latest win32

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/

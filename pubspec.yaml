name: wisdom_pos_test
description: A new Flutter project.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.1.2 <4.0.0"

dependencies:
  flutter_bloc: ^9.1.1
  dio: ^5.9.0
  equatable: ^2.0.7
  flutter:
    sdk: flutter
  google_fonts: ^6.2.0
  shared_preferences: ^2.2.3
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

dependency_overrides:
  win32: ^5.5.4 # 🔥 paksa semua paket pakai win32 terbaru

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/
